.앞에 '이다', '있다' 이외의 OP가 있으면 
ex) a에 f(1,3)을 더한다.
이런식으로 있으며 LLHS = a LHS = f(1,3)
으로 된다.

한줄을 토큰별로 분리해서 Vector<string>에 넣고
처음으로 시작하는 OP의 인덱스를 찾아
뒤로가는 Iterator와 앞으로 토큰을 찾는 Iterator를둠.
함수와 id를 구분하기 위해 정수, 정수형 사용.
